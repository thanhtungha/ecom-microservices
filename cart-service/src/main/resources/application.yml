server:
  port: 0
spring:
  application:
    name: cart-service
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
springdoc:
  swagger-ui:
    path: /api/cart/swagger-ui.html
  api-docs:
    path: /api/cart/v3/api-docs