server:
  port: 0
spring:
  application:
    name: order-service
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
springdoc:
  swagger-ui:
    path: /api/order/swagger-ui.html
  api-docs:
    path: /api/order/v3/api-docs